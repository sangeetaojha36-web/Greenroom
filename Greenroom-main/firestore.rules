rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Match any document for development & simulator records
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
